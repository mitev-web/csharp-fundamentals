using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

//Implement the data structure linked list. Define a 
//class ListItem<T> that has two fields: value (of type T) 
//and nextItem (of type ListItem<T>). Define additionally 
//a class LinkedList<T> with a single field firstElement 
//(of type ListItem<T>).

    class LinkedList
    {
        static void Main(string[] args)
        {
        }
    }

